openapi: 3.1.0
info:
  title: Document Intelligence API
  version: "0.1.0"
  description: |-
    API for the Orpheus document intelligence orchestration.
    From the repository: "The Orpheus System transforms static slides into interactive lecture videos with lifelike professor avatars, combining expressive narration, visual presence, and dynamic content to create engaging, personalized learning experiences."
    License: MIT (see repository).
  termsOfService: "https://github.com/fa25genai/orpheus"
  contact:
    name: "Orpheus Project"
    url: "https://github.com/fa25genai/orpheus/issues/new"
  license:
    name: MIT
    url: "https://opensource.org/licenses/MIT"

servers:
  - url: "http://localhost:30606"
    description: "Local development (random port chosen: 30606)"
  - url: "http://orpheus-service-docint:8080"
    description: "DNS service name for in-cluster service"

tags:
  - name: docint
    description: "Endpoints for document storage and retrieval."

paths:

  /v1/upload/{courseId}:
    post:
      tags:
        - docint
      summary: uploads a PDF document
      operationId: uploadsDocument
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/pdf:
            schema:
              type: string
              format: binary
              description: "Raw PDF file"
      responses:
        "201":
          description: Successfully uploaded. Returns created ID.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadResponse"
        "400": 
          $ref: "#/components/responses/BadRequest"
        "404": 
          $ref: "#/components/responses/NotFound"
        "413": 
          $ref: "#/components/responses/PayloadTooLarge"
        "415": 
          $ref: "#/components/responses/UnsupportedMediaType"

  /v1/video/{courseId}:
    post:
      tags:
        - docint
      summary: uploads a video file
      operationId: uploadsVideo
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          video/mp4:
            schema:
              type: string
              format: binary
              description: "Raw MP4 video file"
      responses:
        "201":
          description: Successfully uploaded. Returns created video ID.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VideoUploadResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"

  /v1/delete/{documentId}:
    delete:
      tags:
        - docint
      summary: Deletes a document
      operationId: deletesDocument
      parameters:
        - $ref: "#/components/parameters/DocumentId"
      responses:
        "200":
          description: "Successful deletion."
        "400": 
          $ref: "#/components/responses/BadRequest"
        "404": 
          $ref: "#/components/responses/NotFound"

  /v1/retrieval/{courseId}:
    get:
      tags: 
        - docint
      summary: Provides relevant textual content and images
      operationId: retrievesDataForGeneration
      parameters:
        - $ref: "#/components/parameters/CourseId"
        - name: promptQuery
          in: query
          required: true
          schema:
            type: string
          description: "The user's query or prompt."
      responses:
        "200":
          description: "Content and images."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetrievalResponse"
        "400": 
          $ref: "#/components/responses/BadRequest"
        "404": 
          $ref: "#/components/responses/NotFound"

  /v1/retrieval/{courseId}/batch:
    post:
      tags:
        - docint
      summary: Provides relevant textual content and images for multiple queries
      operationId: retrievesBatchDataForGeneration
      parameters:
        - $ref: "#/components/parameters/CourseId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchRetrievalRequest"
      responses:
        "200":
          description: "Batch content and images results."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchRetrievalResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

components:
  parameters:
    CourseId:
      name: courseId
      in: path
      required: true
      schema: 
        type: string
      description: "The course ID."
    DocumentId:
      name: documentId
      in: path
      required: true
      schema:
        type: string
      description: "The document ID."

  responses:
    BadRequest:
      description: Bad Request – missing file or parameters.
    NotFound:
      description: Not Found – resource not found.
    PayloadTooLarge:
      description: Payload Too Large.
    UnsupportedMediaType:
      description: Unsupported Media Type (only PDFs and MP4 videos accepted).

  schemas:
    UploadResponse:
      type: object
      required: [documentId]
      properties:
        documentId:
          type: string
          description: The ID of the stored document.

    VideoUploadResponse:
      type: object
      required: [videoId]
      properties:
        videoId:
          type: string
          description: The ID of the stored video.

    RetrievalResponse:
      type: object
      required: [content, images]
      properties:
        content:
          type: array
          items:
            type: string
          description: "Array of text snippets."
        images:
          type: array
          items: 
            $ref: "#/components/schemas/ImageObject"
          description: "Array of image objects."

    BatchRetrievalRequest:
      type: object
      required: [promptQueries]
      properties:
        promptQueries:
          type: array
          items:
            type: string
          description: "Array of user queries or prompts."

    BatchRetrievalResponse:
      type: object
      required: [results]
      properties:
        results:
          type: array
          items:
            $ref: "#/components/schemas/RetrievalResponse"
          description: "Array of retrieval responses, one for each input query."

    ImageObject:
      type: object
      properties:
        image:
          type: string
          description: "Base64-encoded image in data URI format (e.g., 'data:image/jpeg;base64,/9j/4gIoSUNDX1BST0ZJTEUAAQEAAAIYAAAAAAIQAABtbnRyUkdCIFhZWi...')."
          example: "data:image/jpeg;base64,/9j/4gIoSUNDX1BST0ZJTEUAAQEAAAIYAAAAAAIQAABtbnRyUkdCIFhZWi"
        description:
          type: string
          description: "Short description of the image."